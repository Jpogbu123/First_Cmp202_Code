import java.util.InputMismatchException;
import java.util.Scanner;

    public class OOPMain {

        public static void main(String[] args) {
           GeometryUI geometryUI = new GeometryUI();
           geometryUI.mainUI();
        }
    }
           /* geometry.accessingPi();

            Scanner input = new Scanner(System.in);
            try{
                float length = input.nextFloat();
                float breadth = input.nextFloat();

                System.out.println(geometry.areaOfTriangle(breadth, length));
            }catch (InputMismatchException ime){
                System.out.println("Enter a valid number");
            }catch (Exception e){
                System.out.println("Something went wrong");
            }
        }
    }*/
    // How to compile?
    //open terminal =javac src/Main.java
    //TO DO
    //method overridding is called dynamic polymorphism
    //method overloading is called static  polymorphism
//Exception they don't occur when you compile
    //:exception handling

    //to input : float length= input.nextfloat();
    //          float breadth= input.nextfloat();
   // catch

//assignment: create a physic class (private attribute gravity) extends to Energy (kinetic, potential energy)
// create new repository, name,
// put code on git
//constructors and interface